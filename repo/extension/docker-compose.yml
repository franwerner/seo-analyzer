services:
  extension:
    build: .
    container_name: seo-analyzer-extension
    image: seo-analyzer-extension:1.0
    ports:
      - ${PORT}:${PORT}
    env_file:
      - ./.env
    command: npm run dev
    networks:
      - seo-analyzer-network
    develop:
      watch:
        - action: sync
          path: ./src
          target: /app/src
networks:
  seo-analyzer-network:
    external: true
